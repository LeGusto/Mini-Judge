// Solves the Coin Change problem using DP
// outputs the minimum number of coins needed, -1 if impossible.

#include <iostream>

using namespace std;

int main()
{
    string a;
    std::cin >> a;
    std::cout << a << endl;
    return 0;
}