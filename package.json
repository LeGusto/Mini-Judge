{
  "dependencies": {
    "dockerode": "^4.0.7",
    "express": "^5.1.0",
    "jest": "^30.0.3",
    "multer": "^2.0.1",
    "tar": "^7.4.3"
  },
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:unit": "jest --testPathIgnorePatterns=judge.integration.test.js",
    "test:integration": "jest judge.integration.test.js"
  },
  "devDependencies": {
    "fs-extra": "^11.3.0",
    "supertest": "^7.1.3"
  }
}
